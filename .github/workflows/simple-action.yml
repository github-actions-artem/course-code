name: Simple Action

on:
  push:
    
jobs:
  simple-action:
    runs-on: ubuntu-latest
    steps:
      - name: Simple JS Action
        uses: actions/hello-world-javascript-action@v1.1
        with:
          who-to-greet: 'Artem'